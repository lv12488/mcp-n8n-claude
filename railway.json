{
  "build": {
    "env": {
      "NODE_ENV": "production"
    },
    "builder": "nixpacks",
    "command": "npm ci && npm run build"
  },
  "deploy": {
    "startCommand": "node build/index.js"
  }
}